# Set to "true" to use mock data (no backend required).
# Set to "false" or remove to use the live backend API.
VITE_USE_MOCK=true

# API base URL (overridden by Vite proxy in dev, nginx in production).
# Only needed if NOT using the proxy.
# VITE_API_BASE=http://localhost:8000
